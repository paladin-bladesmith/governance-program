/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/web3.js';
import { PALADIN_GOVERNANCE_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getProposalVoteElectionDecoder,
  getProposalVoteElectionEncoder,
  type ProposalVoteElection,
  type ProposalVoteElectionArgs,
} from '../types';

export type SwitchVoteInstruction<
  TProgram extends string = typeof PALADIN_GOVERNANCE_PROGRAM_PROGRAM_ADDRESS,
  TAccountStakeAuthority extends string | IAccountMeta<string> = string,
  TAccountStake extends string | IAccountMeta<string> = string,
  TAccountStakeConfig extends string | IAccountMeta<string> = string,
  TAccountVote extends string | IAccountMeta<string> = string,
  TAccountProposal extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountStakeAuthority extends string
        ? ReadonlySignerAccount<TAccountStakeAuthority> &
            IAccountSignerMeta<TAccountStakeAuthority>
        : TAccountStakeAuthority,
      TAccountStake extends string
        ? ReadonlyAccount<TAccountStake>
        : TAccountStake,
      TAccountStakeConfig extends string
        ? ReadonlyAccount<TAccountStakeConfig>
        : TAccountStakeConfig,
      TAccountVote extends string
        ? WritableAccount<TAccountVote>
        : TAccountVote,
      TAccountProposal extends string
        ? WritableAccount<TAccountProposal>
        : TAccountProposal,
      ...TRemainingAccounts,
    ]
  >;

export type SwitchVoteInstructionData = {
  discriminator: number;
  newElection: ProposalVoteElection;
};

export type SwitchVoteInstructionDataArgs = {
  newElection: ProposalVoteElectionArgs;
};

export function getSwitchVoteInstructionDataEncoder(): Encoder<SwitchVoteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', getU8Encoder()],
      ['newElection', getProposalVoteElectionEncoder()],
    ]),
    (value) => ({ ...value, discriminator: 6 })
  );
}

export function getSwitchVoteInstructionDataDecoder(): Decoder<SwitchVoteInstructionData> {
  return getStructDecoder([
    ['discriminator', getU8Decoder()],
    ['newElection', getProposalVoteElectionDecoder()],
  ]);
}

export function getSwitchVoteInstructionDataCodec(): Codec<
  SwitchVoteInstructionDataArgs,
  SwitchVoteInstructionData
> {
  return combineCodec(
    getSwitchVoteInstructionDataEncoder(),
    getSwitchVoteInstructionDataDecoder()
  );
}

export type SwitchVoteInput<
  TAccountStakeAuthority extends string = string,
  TAccountStake extends string = string,
  TAccountStakeConfig extends string = string,
  TAccountVote extends string = string,
  TAccountProposal extends string = string,
> = {
  /** Paladin stake authority account */
  stakeAuthority: TransactionSigner<TAccountStakeAuthority>;
  /** Paladin stake account */
  stake: Address<TAccountStake>;
  /** Paladin stake config account */
  stakeConfig: Address<TAccountStakeConfig>;
  /** Proposal vote account */
  vote: Address<TAccountVote>;
  /** Proposal account */
  proposal: Address<TAccountProposal>;
  newElection: SwitchVoteInstructionDataArgs['newElection'];
};

export function getSwitchVoteInstruction<
  TAccountStakeAuthority extends string,
  TAccountStake extends string,
  TAccountStakeConfig extends string,
  TAccountVote extends string,
  TAccountProposal extends string,
>(
  input: SwitchVoteInput<
    TAccountStakeAuthority,
    TAccountStake,
    TAccountStakeConfig,
    TAccountVote,
    TAccountProposal
  >
): SwitchVoteInstruction<
  typeof PALADIN_GOVERNANCE_PROGRAM_PROGRAM_ADDRESS,
  TAccountStakeAuthority,
  TAccountStake,
  TAccountStakeConfig,
  TAccountVote,
  TAccountProposal
> {
  // Program address.
  const programAddress = PALADIN_GOVERNANCE_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    stakeAuthority: { value: input.stakeAuthority ?? null, isWritable: false },
    stake: { value: input.stake ?? null, isWritable: false },
    stakeConfig: { value: input.stakeConfig ?? null, isWritable: false },
    vote: { value: input.vote ?? null, isWritable: true },
    proposal: { value: input.proposal ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.stakeAuthority),
      getAccountMeta(accounts.stake),
      getAccountMeta(accounts.stakeConfig),
      getAccountMeta(accounts.vote),
      getAccountMeta(accounts.proposal),
    ],
    programAddress,
    data: getSwitchVoteInstructionDataEncoder().encode(
      args as SwitchVoteInstructionDataArgs
    ),
  } as SwitchVoteInstruction<
    typeof PALADIN_GOVERNANCE_PROGRAM_PROGRAM_ADDRESS,
    TAccountStakeAuthority,
    TAccountStake,
    TAccountStakeConfig,
    TAccountVote,
    TAccountProposal
  >;

  return instruction;
}

export type ParsedSwitchVoteInstruction<
  TProgram extends string = typeof PALADIN_GOVERNANCE_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Paladin stake authority account */
    stakeAuthority: TAccountMetas[0];
    /** Paladin stake account */
    stake: TAccountMetas[1];
    /** Paladin stake config account */
    stakeConfig: TAccountMetas[2];
    /** Proposal vote account */
    vote: TAccountMetas[3];
    /** Proposal account */
    proposal: TAccountMetas[4];
  };
  data: SwitchVoteInstructionData;
};

export function parseSwitchVoteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedSwitchVoteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      stakeAuthority: getNextAccount(),
      stake: getNextAccount(),
      stakeConfig: getNextAccount(),
      vote: getNextAccount(),
      proposal: getNextAccount(),
    },
    data: getSwitchVoteInstructionDataDecoder().decode(instruction.data),
  };
}
